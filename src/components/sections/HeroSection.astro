---
/**
 * HeroSection — Full-viewport opening section
 *
 * NOTE: Intentionally does NOT use the <Section> primitive.
 * Reasons:
 *   - Fullscreen (100dvh), unique radial gradient background
 *   - Uses <h1> + badge pattern instead of <h2> + section-tag
 *   - Has its own entrance animation sequence (staggered CSS)
 *   - <Section> is for scrollable content sections, not the page opener
 */
import { SITE } from "../../data/site";
import Icon from "../ui/Icon.astro";
---

<section class="hero" id="hero" aria-label="Hoofdbanner">
    <div class="hero-bg">
        <div class="hero-gradient"></div>
        <div class="hero-grid"></div>
    </div>

    <div class="container hero-content">
        <div class="hero-badge">
            <span class="badge-dot"></span>
            {SITE.hero.badge}
        </div>

        <h1 class="hero-title">
            <span class="title-line">{SITE.hero.titleLine1}</span>
            <span class="title-line title-accent">{SITE.hero.titleLine2}</span>
        </h1>

        <p class="hero-subtitle">{SITE.hero.subtitle}</p>

        <div class="hero-actions">
            <button
                type="button"
                data-modal-open="contact-modal"
                class="btn btn-primary btn-lg"
            >
                <Icon name="mail" size={18} />
                {SITE.cta.primary.label}
            </button>
            <a
                href={SITE.contact.whatsappHref}
                class="btn btn-secondary btn-lg"
                target="_blank"
                rel="noopener noreferrer"
                aria-label={`Stuur een WhatsApp bericht naar ${SITE.contact.phone}`}
            >
                <Icon name="message-circle" size={18} />
                WhatsApp
            </a>
        </div>

        <div class="hero-bottom-group">
            <div class="hero-stats glass-card" aria-label="Statistieken">
                {
                    SITE.hero.stats.map((stat, i) => (
                        <>
                            {i > 0 && <div class="stat-divider" aria-hidden="true" />}
                        <div class="stat-item">
                            <span class="stat-value">{stat.value}</span>
                            <span class="stat-label">{stat.label}</span>
                        </div>
                    </>
                ))
            }
        </div>

        <div class="hero-subsidie-wrapper">
            <a href="#keurmerken" class="subsidie-pill">
                <div class="pill-icon">
                    <Icon name="check" size={24} strokeWidth={3} />
                </div>
                <span class="pill-text"><strong>ISDE-subsidie</strong> mogelijk op kozijnen en glas</span>
                <Icon name="arrow-right" size={20} class="pill-arrow" />
            </a>
        </div>
    </div>
    </div>
</section>

<style>
    .hero {
        position: relative;
        min-height: 100vh;
        min-height: 100dvh;
        display: flex;
        align-items: center;
        padding: 6rem 0 3rem;
        overflow: hidden;
    }

    @media (min-width: 769px) {
        .hero {
            padding: 8rem 0 4rem;
        }
    }
    .hero-bg {
        position: absolute;
        inset: 0;
        z-index: 0;
    }
    .hero-gradient {
        position: absolute;
        inset: 0;
        background:
            radial-gradient(
                ellipse 80% 60% at 20% 40%,
                rgba(var(--color-cta-rgb), 0.15),
                transparent 60%
            ),
            radial-gradient(
                ellipse 60% 80% at 80% 60%,
                rgba(var(--color-accent-rgb), 0.08),
                transparent 60%
            ),
            radial-gradient(
                ellipse 100% 50% at 50% 100%,
                rgba(var(--color-cta-rgb), 0.08),
                transparent
            );
    }
    .hero-grid {
        position: absolute;
        inset: 0;
        background-image:
            linear-gradient(rgba(var(--color-primary-rgb), 0.06) 1px, transparent 1px),
            linear-gradient(90deg, rgba(var(--color-primary-rgb), 0.06) 1px, transparent 1px);
        background-size: 60px 60px;
        mask-image: radial-gradient(
            ellipse 80% 60% at 50% 40%,
            black 20%,
            transparent 70%
        );
        -webkit-mask-image: radial-gradient(
            ellipse 80% 60% at 50% 40%,
            black 20%,
            transparent 70%
        );
    }

    /* Dark mode: reduce grid opacity so it doesn't create a blue vignette */
    :global([data-theme="dark"]) .hero-grid {
        background-image:
            linear-gradient(rgba(255, 255, 255, 0.025) 1px, transparent 1px),
            linear-gradient(90deg, rgba(255, 255, 255, 0.025) 1px, transparent 1px);
    }
    .hero-content {
        position: relative;
        z-index: 1;
        max-width: 700px;
    }
    .hero-badge {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.5rem 1.25rem;
        background: var(--surface-card);
        border: 1px solid var(--border-card);
        border-radius: var(--radius-full);
        font-size: 0.8125rem;
        font-weight: 600;
        color: var(--color-cta);
        margin-bottom: 1.5rem;
        animation: fadeInUp 0.6s ease-out;
        box-shadow: var(--shadow-sm);
    }
    .badge-dot {
        width: 7px;
        height: 7px;
        background: var(--color-accent);
        border-radius: 50%;
        animation: pulse-dot 2s infinite;
        box-shadow: 0 0 6px rgba(var(--color-accent-rgb), 0.5);
    }
    .hero-title {
        font-size: clamp(2.5rem, 5.5vw, 4.25rem);
        font-weight: 800;
        letter-spacing: -0.03em;
        margin-bottom: 1.5rem;
        animation: fadeInUp 0.6s ease-out 0.1s both;
    }
    .title-line {
        display: block;
    }
    .title-accent {
        background: linear-gradient(
            135deg,
            var(--color-cta),
            var(--color-cta-light),
            var(--color-accent)
        );
        background-size: 200% auto;
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
    .hero-subtitle {
        font-size: clamp(1rem, 2.5vw, 1.1875rem);
        color: var(--text-secondary);
        max-width: 560px;
        margin-bottom: 2.25rem;
        line-height: 1.75;
        animation: fadeInUp 0.6s ease-out 0.2s both;
    }
    .hero-actions {
        display: flex;
        flex-wrap: wrap;
        gap: 0.875rem;
        margin-bottom: 3rem;
        animation: fadeInUp 0.6s ease-out 0.3s both;
    }
    /* Bottom Group (Stats + Pill) */
    .hero-bottom-group {
        display: inline-flex;
        flex-direction: column;
        align-items: flex-start; /* Default left-aligned */
    }

    .hero-stats {
        display: flex;
        /* Mobile base: stacked column layout */
        flex-direction: column;
        gap: 1rem;
        align-items: flex-start;
        padding: 1.25rem 1.5rem;
        animation: fadeInUp 0.6s ease-out 0.4s both;
    }
    .stat-item {
        display: flex;
        flex-direction: column;
        gap: 0.125rem;
    }
    .stat-value {
        font-family: var(--font-heading);
        font-size: clamp(1.25rem, 3vw, 1.5rem);
        font-weight: 800;
        color: var(--color-cta);
        line-height: 1.1;
    }
    .stat-label {
        font-size: clamp(0.6875rem, 1.2vw, 0.75rem);
        font-weight: 500;
        color: var(--text-muted);
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }
    /* Mobile base: horizontal divider */
    .stat-divider {
        width: 100%;
        height: 1px;
        background: var(--border-subtle);
    }

    /* ─── Reduced Motion ─────────────────────────────────── */
    @media (prefers-reduced-motion: reduce) {
        .hero-badge,
        .badge-dot,
        .hero-title,
        .hero-subtitle,
        .hero-actions,
        .hero-stats,
        .hero-subsidie-wrapper {
            animation: none !important;
        }
    }

    /* Desktop (>=769px): row layout, vertical dividers */
    @media (min-width: 769px) {
        .hero-bottom-group {
            align-items: center; /* Center the pill precisely under the stats row */
        }
        .hero-stats {
            flex-direction: row;
            align-items: center;
            gap: 2rem;
            padding: 1.5rem 2.5rem;
        }
        .stat-divider {
            width: 1px;
            height: 2.5rem;
        }
    }

    /* ─── Ultra Prominent Subsidie Banner ─────────────────────────────────── */
    .hero-subsidie-wrapper {
        margin-top: 2rem; /* bit more breathing room */
        display: flex;
        width: 100%;
        justify-content: center; /* Center horizontally inside bottom group */
        animation: fadeInUp 0.6s ease-out 0.5s both;
    }

    .subsidie-pill {
        display: inline-flex;
        align-items: center;
        background: linear-gradient(135deg, var(--color-cta), var(--color-cta-dark));
        border: 2px solid rgba(255,255,255,0.15); /* Light inner edge feeling */
        border-radius: var(--radius-full);
        padding: 0.5rem 1.75rem 0.5rem 0.5rem;
        text-decoration: none;
        box-shadow: 0 8px 24px rgba(var(--color-cta-rgb), 0.35);
        color: #ffffff;
        transform-origin: center;
        animation: breathSubsidie 3.5s infinite ease-in-out;
        transition: transform var(--transition-normal), box-shadow var(--transition-normal), filter var(--transition-normal);
    }

    @keyframes breathSubsidie {
        0%, 100% { transform: scale(1); box-shadow: 0 4px 15px rgba(var(--color-cta-rgb), 0.3); }
        50% { transform: scale(1.02); box-shadow: 0 12px 30px rgba(var(--color-cta-rgb), 0.5); }
    }

    .subsidie-pill:hover {
        filter: brightness(1.1);
        transform: scale(1.04) !important; /* Overrides breath animation briefly on hover */
        box-shadow: 0 12px 32px rgba(var(--color-cta-rgb), 0.6);
        animation-play-state: paused;
    }

    .pill-icon {
        display: flex;
        align-items: center;
        justify-content: center;
        width: 3rem;
        height: 3rem;
        background: #ffffff;
        color: var(--color-cta);
        border-radius: 50%;
        flex-shrink: 0;
        margin-right: 1.25rem;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
    }

    .pill-text {
        font-size: 1.0625rem; /* Larger font */
        font-weight: 500;
        margin-right: 0.75rem;
        color: #ffffff;
    }

    .pill-text strong {
        font-weight: 800;
        color: #ffffff;
        letter-spacing: 0.02em;
        text-transform: uppercase;
    }

    .pill-arrow {
        color: rgba(255, 255, 255, 0.9);
        transition: transform var(--transition-normal);
        margin-left: 0.5rem;
    }

    .subsidie-pill:hover .pill-arrow {
        transform: translateX(6px);
    }
</style>
